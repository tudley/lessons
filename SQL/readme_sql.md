# SQL Cheat sheet

## Reading from a single Table:

```
    SELECT * FROM db.Table
```

Here, SELECT \* grabs all columns for every mathcing row in the selected table

Which can be filtered to a set amount with

```
    SELECT TOP 5 * FROM db.Table
```

To fetch the first 5 results from the SELECT query
